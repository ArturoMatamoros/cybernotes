[[Network]]
Il proxy è un software che si interpone tra il server e il browser. Quindi fa da intermediario. Questa è utile per filtrare, proteggere il browser e l'utente, controlla il contenuto ecc. Questa può fare mille cose, anche iniettare del codice. 

Il browser di [[burpsuite]] è un normale browser che utilizza i proxy. 