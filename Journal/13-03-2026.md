[[ 2026-03-12]] - [[# 2026-03-14]]
## Today

## Captures
- Studying how to pass the level high in [[File upload vulnerability]] 
- [[PEN testing tools]] 
	- whatweb --aggression 3 -vv https://itsaltoadriatico
	- sslscan itsaltoadriatico
	- ffuf word/words/ecc itsaltoadriatico.it/fuzz -mc 200
		- this is a fuzzing web that test's  every word that we give input and checks if exists 
	- xsser --url "http://vulnerable.arxway.cloud/v0post.php" -p "name=XSS" -Xsr
		- this one checks if the site is vulnerable to cross site scripting 
		- you have to insert the cookie and more info
	- commix --url  "http://vulnerable.arxway.cloud/v0post.php" --data="" --cookie=""
		- same thing but for command injection, if vulnerable it opens a terminal inside
	- sqlmap --cookie="" --url="" (options)
		- same thing but for [[SQL Injection]] 
	- hydra -I admin -P /usr/share/eaphammer/wordlist/rockyou.txt -u -f 192.168.1.11 -s 9001 http-get-form "/vulnerabilities/brute: username=^USER^&&password=^PASS^&&Login=Login :H=Cookie\ ecc:"
		- Here we do a brute force
		- options
			- ftp 
			- ssh
	- john the ripper 
		- tries to find passwords from the hash 
		- it can also work for zip files 
	- crunch 6 7 abc123 -o wordlist.txt 
	- hashcat -m 1800 -a 0 -o cracked.txt hash.txt /usr/share/eaphammer/wordlists/rockyou 
		- same, but remember that it's almost impossible to actually find the right passwords 
## To process
- [ ] [[Report demo tools]]
- [ ] keywords to look up
- [ ] labs to add to task bank