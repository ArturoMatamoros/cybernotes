[[ 2026-03-09]] - [[# 2026-03-11]]
## Today

## Captures
- BOM
	- They are entities that we made the part that we can model with JS 
	- we also have the methods resize to control the windows 
		- resizeTo and resizeBy
	- the Navigator is an object that gives me information about the browser
		- an example the google name uses it to know if there is connection
``` javascript
	function getInfoBrowser() {
		alert(
			"ilbrowser è in un contesto con queste caratteristiche"
			+ "\n" + 
			"Nome del Browser: " + navigator.appName + "\n" +
			"Codice del Browser: " + navigator.appCodeName + "\n" +
			"Verisione del browser: " + navigator.appVersion + "\n" +
			"HTTP " + 
		);
	}
```

- HTTP 
	- e-mail
		- IMAP, POP3 
			- protocols to download the mail
		- SMTP
	- HTTP vs HTTPS 
	- Rules of the HTTP protocol
		- type of communication
		- form of the answers
		- security mechanism 
	- Ports
		- 80 (the typical HTTP)
		- 443 (The new standard HTTPS)
	- Parameters 
## To process
- [ ] things to turn into zettelkasten notes
- [ ] keywords to look up
- [ ] labs to add to task bank